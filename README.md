# automarket_mexico

A new Flutter project.


ngrok http 8000

flutter run

php artisan serve --host=0.0.0.0 --port=8000

ngrok http --scheme=http --host-header=localhost:8000

git add . && git commit -m "actualizacion" && git push